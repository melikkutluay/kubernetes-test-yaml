apiVersion: v1
kind: Pod
metadata:
  name: pd-dap
spec:
  containers:
    - name: dap
      image: melikktly/demo:2.0.0
      ##image: melikktly/spring-demo:2.0.0
      ports:
        - containerPort: 80